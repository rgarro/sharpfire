<%= form_for(@tecnica) do |f| %>
  <% if @tecnica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tecnica.errors.count, "error") %> prohibited this tecnica from being saved:</h2>

      <ul>
      <% @tecnica.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
